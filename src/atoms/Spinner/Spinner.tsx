import React from 'react';
import { Oval } from 'react-loader-spinner';

const Spinner = (): React.ReactElement => (
  <Oval color="#fff" secondaryColor="#000" height={80} width={80} />
);

export default Spinner;
